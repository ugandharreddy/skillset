<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.attra.SkillProficiencyClient</groupId>
    <artifactId>SkillProficiencyClient</artifactId>
    <packaging>war</packaging>

    <!-- SNAPSHOT means it goes to Nexus snapshots repo -->
    <version>0.0.1-SNAPSHOT</version>

    <name>SkillProficiencyClient Maven Webapp</name>
    <url>http://maven.apache.org</url>

    <properties>
        <junit-version>3.8.1</junit-version>
        <org.springframework-version>4.0.5.RELEASE</org.springframework-version>
        <mysql-version>5.1.15</mysql-version>
        <jackson.databind-version>2.3.0</jackson.databind-version>
        <org.aspectj-version>1.7.4</org.aspectj-version>
        <javax.inject-version>1</javax.inject-version>
        <javax.servlet-api-version>2.5</javax.servlet-api-version>
        <javax.servlet.jsp-version>2.1</javax.servlet.jsp-version>
        <javax.servlet-jstl-version>1.2</javax.servlet-jstl-version>
        <org.apache.maven.plugins-version>2.9</org.apache.maven.plugins-version>
        <org.codehaus.mojo-version>1.2.1</org.codehaus.mojo-version>
        <java-source>1.7</java-source>
        <java-target>1.7</java-target>
        <hibernate-version>4.2.5.Final</hibernate-version>
        <dbcp.version>1.4</dbcp.version>
        <org.slf4j-version>1.7.5</org.slf4j-version>
        <spring-security-version>3.2.4.RELEASE</spring-security-version>
    </properties>

    <dependencies>

        <!-- ALL YOUR EXISTING DEPENDENCIES (UNCHANGED) -->
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi</artifactId>
            <version>3.7</version>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-all</artifactId>
            <version>1.9.5</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.12</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-core</artifactId>
            <version>${org.springframework-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <version>${org.springframework-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-core</artifactId>
            <version>${spring-security-version}</version>
        </dependency>

        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>${mysql-version}</version>
        </dependency>

        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-core</artifactId>
            <version>${hibernate-version}</version>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.databind-version}</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${org.apache.maven.plugins-version}</version>
                <configuration>
                    <source>${java-source}</source>
                    <target>${java-target}</target>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
            </plugin>

        </plugins>

        <finalName>SkillProficiencyClient</finalName>
    </build>

    <!-- 🔥 NEXUS CONFIGURATION ADDED -->
    <distributionManagement>

        <repository>
            <id>nexus-releases</id>
            <url>http://44.200.57.40:8081/repository/maven-releases/</url>
        </repository>

        <snapshotRepository>
            <id>nexus-snapshots</id>
            <url>http://44.200.57.40:8081/repository/maven-snapshots/</url>
        </snapshotRepository>

    </distributionManagement>

</project>
